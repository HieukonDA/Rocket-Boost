using UnityEngine;

public interface IBackgroundManager
{
    // void GenerateBackground(int levelNumber, int seed);
    // void UpdateBackgroundPosition(Vector3 playerPosition);
    // void RecycleBackground(GameObject background);
    // GameObject GetLastBackground();
    // void SetBackgroundSpeed(float speed);
    // float GetBackgroundSpeed();
}
